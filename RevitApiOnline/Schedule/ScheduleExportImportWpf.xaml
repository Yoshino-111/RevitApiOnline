<Window x:Class="RevitApiOnline.Schedule.ScheduleExportImportWpf"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Schedule export / import"
        Height="450"
        Width="900"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: schedules -->
        <GroupBox Header="Schedules in model"
                  Margin="0,0,8,0">
            <DataGrid ItemsSource="{Binding Schedules}"
                      SelectedItem="{Binding SelectedSchedule, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"
                                        Binding="{Binding Name}" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Right: fields -->
        <GroupBox Grid.Column="1"
                  Header="Fields in selected schedule">
            <DataGrid ItemsSource="{Binding Fields}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Field name"
                                        Binding="{Binding Name}" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Bottom area -->
        <Grid Grid.Row="1"
              Grid.ColumnSpan="2"
              Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Status + progress -->
            <StackPanel Grid.Row="0"
                        Orientation="Vertical">
                <TextBlock Text="{Binding StatusText}"
                           Margin="0,0,0,2" />
                <ProgressBar Height="16"
                             Minimum="0"
                             Maximum="100"
                             Value="{Binding Progress}" />
            </StackPanel>

            <!-- Key parameter + buttons -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Margin="0,8,0,0">
                <TextBlock Text="Key parameter:"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0" />
                <ComboBox Width="220"
                          ItemsSource="{Binding KeyParameters}"
                          SelectedItem="{Binding SelectedKeyParameter, Mode=TwoWay}" />
                <Button Content="Generate keys"
                        Width="110"
                        Margin="8,0,0,0"
                        Click="ButtonGenerateKeys_Click" />
                <Button Content="Clear keys"
                        Width="90"
                        Margin="8,0,0,0"
                        Click="ButtonClearKeys_Click" />
            </StackPanel>

            <!-- Export / import / close -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,8,0,0">
                <Button Content="Export CSV"
                        Width="90"
                        Margin="0,0,8,0"
                        Click="ButtonExportCsv_Click" />
                <Button Content="Import CSV"
                        Width="90"
                        Margin="0,0,8,0"
                        Click="ButtonImportCsv_Click" />
                <Button Content="Close"
                        Width="80"
                        Click="ButtonClose_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
